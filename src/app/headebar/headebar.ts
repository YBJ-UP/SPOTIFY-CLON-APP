import { Component } from '@angular/core';
import { SpotifySearch } from '../services/spotify-search';

@Component({
  selector: 'app-headebar',
  standalone: false,
  templateUrl: './headebar.html',
  styleUrl: './headebar.css'
})
export class Headebar {
}
